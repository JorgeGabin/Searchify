import React from 'react';
import { SimpleSearch } from '../../search';

const Home = () => {

    return (
        <React.Fragment>
            <SimpleSearch initialQuery=''/>
        </React.Fragment>
    )
};

export default Home;
